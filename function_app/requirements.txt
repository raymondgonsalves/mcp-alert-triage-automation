# Azure Functions Python worker
azure-functions

# Auth: DefaultAzureCredential (dev identity locally, managed identity in Azure)
azure-identity

# HTTP calls to the Sentinel REST API
requests

# The Function app needs the Azure Monitor Query SDK to run KQL from Python
azure-monitor-query

# anthropic — bounded LLM narrator : explains the deterministic severity; never decides it
anthropic

